import React from 'react';
import './nav.scss';


const Layout= (props) => {
    return (
        <div onClick={props.closeToggleBtn} className="layout-wrapper">

        </div>
        
    )
}

export default Layout;